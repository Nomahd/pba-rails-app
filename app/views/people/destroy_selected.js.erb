<% unless params[:selected_audiomessenger].nil? %>
var rows = <%= raw params[:selected_audiomessenger] %>
<% end %>
<% unless params[:selected_devotionmessenger].nil? %>
var rows = <%= raw params[:selected_devotionmessenger] %>
<% end %>
<% unless params[:selected_videomessenger].nil? %>
var rows = <%= raw params[:selected_videomessenger] %>
    <% end %>
<% unless params[:selected_videoguest].nil? %>
var rows = <%= raw params[:selected_videoguest] %>
    <% end %>

    rows.forEach(function(id) {
        var row = document.querySelector("tr[id='" + id + "']")
        row.parentNode.removeChild(row)
    })

